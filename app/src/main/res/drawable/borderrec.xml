<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/lightgray" /> <!-- Cambia el color de fondo a gris claro -->
    <stroke
        android:width="3dp"
        android:color="@color/black"/> <!-- Color del borde -->
    <corners android:radius="8dp" /> <!-- Radio de esquina, ajusta según desees -->
    <item android:left="-2dp" android:top="-2dp">
        <shape>
            <size android:width="20dp" android:height="20dp" />
            <solid android:color="@color/red" /> <!-- Color del pico -->
            <corners android:radius="2dp" /> <!-- Radio de esquina del pico -->
        </shape>
    </item>
    <item android:right="-2dp" android:top="-2dp">
        <shape>
            <size android:width="20dp" android:height="20dp" />
            <solid android:color="@color/red" /> <!-- Color del pico -->
            <corners android:radius="2dp" /> <!-- Radio de esquina del pico -->
        </shape>
    </item>
</shape>
